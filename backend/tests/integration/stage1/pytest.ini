[pytest]
testpaths = tests
markers =
    live: requires a reachable backend API (LARK_PLAT_API_BASE)
    seed: requires seed data (permission points + three roles + group A/B hosts)
    openapi: static check against committed docs/openapi.json
addopts = -ra --strict-markers
