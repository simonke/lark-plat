# 批次验收清单模板（acceptance checklist）

> 每批验收填写本表；「本批未覆盖面」为**必填**栏，空＝验收不成立。
> 纪律见 `docs/live-acceptance-policy.md`（反盲区六条）。

## 0. 锚

- 批次名 / 分支 / **clean committed SHA（接受证据的锚）**：
- merge-base / off 基线：
- 是否 push / 远端绑定：
- 是否触共享环境（DB/Redis/进程/`：8000`）：

## 1. 证据面矩阵（**开场冻结**，每条必标面；收口不得删项/事后追认）

| 要件 | 判据 | 证据面 | 产方 | 证方（≠产方） | 结论(PASS/BLOCKED/NOT RUN) | 证据 |
|------|------|--------|------|----------------|------------------------------|------|
|      |      | 单元锁 / in-process / 契约 / live-served / 共享环境 |  |  |  |  |

- 触库批次必须有 ≥1 行落在 `live-served` 或 `共享环境`。
- 环境实例身份：`live-served`＝**PID＋served HEAD**；`共享环境`＝**DB revision（`alembic_version`）＋实例标识（端口/库名）**。
- **证方 ≠ 产方**：某面 producer 不得自证该面。

## 2. live 就绪冒烟（开场＋收口各一次）

- `python backend/tools/live_readiness_smoke.py`（输出 `contract` / `db_rev` / `db_touch`）：
- live 结论附件：**PID＝** / **served HEAD＝** / **DB `alembic_version`＝**

## 3. 非空判 / 负控制（预核要件）

- 本批门禁「能变红」的证明（前置 SHA / 真实漂移 / 故意违规）：**未演示⇒预核不予绑定**。
- 门禁清单及其红/绿证据：

## 4. 未覆盖面（必填，禁空；需求席登记）

- 本批**未做**的验证面：
- 未覆盖项去向：显式 **列 C / 后续批**（**需求席登记入账本**；收口报告必须引用；不得隐式略过）。

## 5. 措辞边界

- 本批**不得**宣称：`（如：全功能绿 / live 端到端通过）`
- 可宣称范围：

## 6. 签核

- 需求（判据/证据纪律）：
- 代码reviewer（独立复核/负控制）：
- 架构（判据层级/live 就绪门）：
